# Keyboard shortcuts

Every key the editor answers to, in one table. It is the same table **Help ▾** › **Keyboard shortcuts…** (or **?**) shows — generated from one list in the code, not written here — so the two can never disagree, and the arrow-key steps read the sizes you chose in [Settings](settings.md#step-sizes-playhead-nudge-and-jump).

## Every shortcut

```shortcuts
```

The transport keys are explained on the Preview and playback page; Undo and Redo on the Undo and redo page; Escape's other job, leaving a previewed library clip, on [Preview a clip](media-library.md#preview-a-clip).

## When the shortcuts pause

The keys do nothing while you are typing in a text field, while a button, slider, drop-down or link has focus and would answer the key itself, and while any dialog is open — the export dialog, a confirmation, the project dialogs, the cheat sheet itself. Move focus off the control, or close the dialog, and they are back. The undo chords wait behind a dialog too, but not behind a focused control — Ctrl+Z works over a button or a slider — and inside text fields Ctrl+Z is the browser's own text undo.
